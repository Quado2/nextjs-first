//import {MongoClient} from 'mongodb'

//This is how we connect to mongodb. the name of the 
// the username, password and name of the database is encoded in the 
// connection url

// async function dbConnection(){
//     const client = await MongoClient.connect('mongodb+srv://quado:qasw1234@cluster0.ggutm.mongodb.net/meetup?retryWrites=true&w=majority')
//     const db = client.db();
//     return db
// }



// export default dbConnection